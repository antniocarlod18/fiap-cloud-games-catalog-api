apiVersion: v1
kind: Service
metadata:
    name: antniocarlodfiapcloudgamescatalogapi
spec:
    type: LoadBalancer
    ports:
    - port: 8080 
    selector:
        app: antniocarlodfiapcloudgamescatalogapi